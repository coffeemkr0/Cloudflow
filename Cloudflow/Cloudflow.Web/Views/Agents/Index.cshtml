@model Cloudflow.Web.ViewModels.Agents.IndexViewModel

@{
    ViewBag.Title = "Agent Explorer";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section headScripts {
    @Scripts.Render("~/bundles/signalr")
    <script src="~/Scripts/Agents/Index.js"></script>
}

<script>
    AgentControllerClient.Agents = [];
    @foreach (var agent in Model.AgentConfigurations)
    {
    <text>
    AgentControllerClient.Agents.push({ machineName: "@agent.MachineName", port: @agent.Port });
    </text>
    }
</script>

<section id="AgentsIndex">
    <header class="page-header">
        <h3>Agent Explorer</h3>
    </header>

    <section class="col-sm-3">
        <ul class="nav nav-list">
            <li class="nav-header">Agents</li>
            <li class="divider"></li>
            <li><a href="#">All</a></li>
            <li class="divider"></li>
            @foreach (var agent in Model.AgentConfigurations)
            {
                <li><a href="#">@agent.MachineName</a></li>
            }
        </ul>
    </section>
    

    <section class="col-sm-9">
        <ul class="nav nav-tabs">
            <li>
                <a href="#queuedTabPane" data-toggle="tab">Queued</a>
            </li>
            <li class="active">
                <a href="#completedTabPane" data-toggle="tab">Completed</a>
            </li>
        </ul>

        <section class="tab-content clearfix">
            <setion id="queuedTabPane" class="tab-pane">
                <table id="queuedGrid" class="table table-striped table-bordered">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Job</th>
                            <th>Started</th>
                        </tr>
                    </thead>
                    <tbody></tbody>
                </table>
            </setion>

            <section id="completedTabPane" class="tab-pane active">
                <table id="completedGrid" class="table table-striped table-bordered">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Job</th>
                            <th>Sarted</th>
                            <th>Completed</th>
                        </tr>
                    </thead>
                    <tbody></tbody>
                </table>
            </section>
        </section>
    </section>
</section>